{
  "name": "bark-coding",
  "description": "A Fun block-based coding editor similar to Scratch.",
  "scripts": {
    "dev": "nw editor/",
    "prod": "nwbuild --platforms win64,linux64,osx64 --outDir export --glob false editor --appName BarkCoding --appVersion 0.1"
  },
  "main": "index.html",
  "version": "0.1",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nw": "^0.89.0",
    "nw-builder": "^3.8.9"
  }
}
