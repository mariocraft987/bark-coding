{
  "name": "bark-coding",
  "version": "0.1.0",
  "description": "A Fun block-based coding editor similar to Scratch.",
  "scripts": {
    "dev": "nw editor/",
    "prod": "nwbuild --mode build --platforms win64,linux64,osx64 --outDir export editor --appName BarkCoding --appVersion 0.1 --nwJsVersion 0.89.0 --src editor"
  },
  "main": "index.html",
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "nw": "^0.89.0",
    "nw-builder": "^3.8.9"
  }
}
